name: Deploy to server

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        run: echo "Hello world"

  second_job:
    if: ${{ github.ref == 'refs/heads/master' }}
    needs: first_job
    runs-on: ubuntu-latest

    steps:
      - name: Hello world
        run: echo "Hello world segundo"